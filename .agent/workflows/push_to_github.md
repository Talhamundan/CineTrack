---
description: Changes to GitHub
---

1. Check status
   > git status

2. Stage all changes
   > git add .

3. Commit changes (Prompt user for message if not provided, otherwise use generic)
   > git commit -m "chore: update project files"

4. Push to remote
   > git push origin main
